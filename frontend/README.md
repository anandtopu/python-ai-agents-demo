# Frontend (Vite + React)

## Run

```powershell
npm install
npm run dev
```

## Backend URL

The UI expects the backend at:

- `http://127.0.0.1:8000`

You can change this by editing `src/App.tsx` (`API_BASE`).
